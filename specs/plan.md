# AutoVideoSlice 技术方案

> 描述 How（如何实现）：系统架构与技术选型

---

## 📐 系统架构

### 整体架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                        前端 (Web UI)                             │
│                    Apple 简约风格界面                             │
├─────────────────────────────────────────────────────────────────┤
│                       本地 HTTP API                              │
├──────────────┬──────────────┬──────────────┬───────────────────┤
│   字幕解析    │   AI 分析    │   视频处理   │     文件管理       │
│    模块      │     模块     │     模块     │       模块         │
├──────────────┴──────────────┴──────────────┴───────────────────┤
│                      核心调度层                                  │
├─────────────────────────────────────────────────────────────────┤
│  FFmpeg     │   LLM API    │   本地存储   │    配置管理         │
│  (视频处理)  │  (内容分析)   │   (文件系统)  │    (JSON)          │
└─────────────────────────────────────────────────────────────────┘
```

### 模块划分

| 模块         | 职责                  | 输入              | 输出                          |
| ------------ | --------------------- | ----------------- | ----------------------------- |
| **字幕解析** | 解析 SRT/VTT 字幕文件 | 字幕文件路径      | 结构化字幕数据（时间戳+文本） |
| **AI 分析**  | 调用大模型分析内容    | 字幕文本          | 高光片段、标题建议、评分      |
| **视频处理** | 视频切割与导出        | 视频文件 + 时间段 | 切割后的视频片段              |
| **文件管理** | 管理输入输出文件      | 用户操作          | 文件列表、导出包              |

### 数据流

```
用户选择视频 + 字幕
        ↓
    字幕解析模块
        ↓
  结构化字幕数据（带时间戳）
        ↓
     AI 分析模块 ──────────────→ 爆款标题建议
        ↓                           ↓
   高光片段列表（时间段 + 评分）      展示给用户
        ↓
   用户确认/调整
        ↓
    视频处理模块
        ↓
   切割后的视频片段
        ↓
    导出（单个/合集）
```

---

## 🛠️ 技术选型

### 前端

| 选项        | 选择             | 理由                                 |
| ----------- | ---------------- | ------------------------------------ |
| 框架        | **React + Vite** | 快速构建、热更新、生态丰富           |
| 样式        | **原生 CSS**     | Apple 风格需要精细控制，不依赖 UI 库 |
| 状态管理    | **Zustand**      | 轻量简单，适合中小项目               |
| HTTP 客户端 | **fetch API**    | 原生支持，无需额外依赖               |

**Apple 风格设计要点**：

- 大量留白
- San Francisco 字体风格（使用 -apple-system 字体栈）
- 微妙的阴影和圆角
- 简洁的黑白灰配色 + 少量强调色
- 流畅的过渡动画

### 后端

| 选项     | 选择                           | 理由                            |
| -------- | ------------------------------ | ------------------------------- |
| 语言     | **Python 3.11+**               | AI/视频处理生态最佳             |
| 框架     | **FastAPI**                    | 高性能、自动 API 文档、异步支持 |
| 视频处理 | **FFmpeg (via ffmpeg-python)** | 行业标准、功能全面              |
| 字幕解析 | **pysrt / webvtt-py**          | 成熟稳定的字幕解析库            |

### AI 模型

| 场景     | 选择                        | 理由                       |
| -------- | --------------------------- | -------------------------- |
| 云端 API | **豆包 doubao-1.5-pro-32k** | 中文理解更强，抖音平台原生 |
| 本地模型 | **Ollama + qwen3:4b**       | 轻量快速，适合本地运行     |

**模型调用策略**：

- 默认尝试本地 Ollama (qwen3:4b)
- 本地不可用时自动回退到豆包 API
- 用户可在设置中选择偏好

---

## ⚠️ 开发规范

### 代码组织规范

> [!IMPORTANT] > **模块化优先，禁止单体巨文件**
>
> - 每个功能模块拆分为独立文件
> - 单个文件不超过 300 行
> - 相关功能按目录组织

### 开发流程规范

1. **写代码前**：必须完整阅读 `specs/requirements.md`
2. **完成里程碑后**：必须更新 `specs/plan.md` 和 `specs/progress.md`
3. **代码审查**：重大变更需记录在文档中

### 环境规范

| 项目     | 要求                           |
| -------- | ------------------------------ |
| 操作系统 | Windows 11                     |
| 终端     | PowerShell                     |
| Python   | 使用 **uv** 管理虚拟环境和依赖 |
| Node.js  | 使用 npm 管理依赖              |

**uv 环境管理命令**：

```powershell
# 创建虚拟环境
uv venv

# 激活虚拟环境
.\.venv\Scripts\Activate.ps1

# 安装依赖
uv pip install -r requirements.txt

# 或使用 pyproject.toml
uv pip install -e .
```

### 项目结构

```
AutoVideoSlice/
├── frontend/                # 前端代码
│   ├── src/
│   │   ├── components/      # UI 组件
│   │   ├── pages/           # 页面
│   │   ├── stores/          # 状态管理
│   │   ├── services/        # API 调用
│   │   └── styles/          # 样式文件
│   └── index.html
│
├── backend/                 # 后端代码
│   ├── api/                 # API 路由
│   ├── services/            # 业务逻辑
│   │   ├── subtitle.py      # 字幕解析
│   │   ├── ai_analyzer.py   # AI 分析
│   │   ├── video_processor.py # 视频处理
│   │   └── file_manager.py  # 文件管理
│   ├── models/              # 数据模型
│   └── main.py              # 入口文件
│
├── specs/                   # 规格文档
│   ├── requirements.md      # 需求文档
│   └── plan.md              # 本文档
│
└── output/                  # 导出目录
```

### 文件作用说明

| 文件/目录                                 | 作用                              | 依赖关系             |
| ----------------------------------------- | --------------------------------- | -------------------- |
| `backend/__init__.py`                     | 后端包入口，统一导出模块          | -                    |
| `backend/main.py`                         | FastAPI 入口，注册路由和中间件    | config, api/\*       |
| `backend/config.py`                       | 配置管理，环境变量支持            | -                    |
| `backend/.env.example`                    | 环境变量示例                      | -                    |
| `backend/api/`                            | API 路由层，处理 HTTP 请求        | services, models     |
| `backend/api/subtitle.py`                 | 字幕解析 API (/parse, /upload)    | services, models     |
| `backend/services/`                       | 业务逻辑层，核心功能实现          | models               |
| `backend/services/subtitle.py`            | SRT/VTT 解析器实现                | models               |
| `backend/models/`                         | 数据模型定义 (Pydantic)           | -                    |
| `backend/models/subtitle.py`              | 字幕数据结构定义                  | -                    |
| `backend/pyproject.toml`                  | 项目配置，定义依赖和构建规则      | -                    |
| `backend/.venv/`                          | Python 虚拟环境 (uv 管理)         | -                    |
| `frontend/src/index.css`                  | Apple 风格 CSS 设计系统           | -                    |
| `frontend/src/App.jsx`                    | 主应用组件                        | components, services |
| `frontend/src/components/Layout.jsx`      | 页面布局组件 (header/main/footer) | -                    |
| `frontend/src/components/FileUpload.jsx`  | 文件上传组件（支持拖拽）          | -                    |
| `frontend/src/components/ScoreCard.jsx`   | 评分卡片组件（环形进度条+维度图） | -                    |
| `frontend/src/components/ScoreCard.css`   | 评分卡片样式                      | -                    |
| `frontend/src/services/api.js`            | 后端 API 服务封装                 | -                    |
| `frontend/vite.config.js`                 | Vite 构建配置                     | -                    |
| `frontend/package.json`                   | npm 依赖和脚本配置                | -                    |
| `backend/services/ai/base.py`             | AI 服务基类（统一接口）           | -                    |
| `backend/services/ai/ollama.py`           | Ollama 本地模型客户端             | base.py              |
| `backend/services/ai/doubao.py`           | 豆包云端 API 客户端               | base.py              |
| `backend/services/ai/manager.py`          | AI 管理器（自动切换逻辑）         | ollama.py, doubao.py |
| `backend/api/ai.py`                       | AI 状态检查和测试 API             | services/ai          |
| `backend/models/analysis.py`              | 分析结果数据模型                  | -                    |
| `backend/services/prompts.py`             | Prompt 模板（高光提取、标题生成） | -                    |
| `backend/services/analyzer.py`            | 内容分析服务                      | ai, prompts          |
| `backend/api/analyze.py`                  | 分析 API 路由                     | analyzer             |
| `backend/models/scoring.py`               | 评分数据模型（维度、等级）        | -                    |
| `backend/services/scorer.py`              | 评分服务（多维度规则评分）        | models/scoring       |
| `backend/api/score.py`                    | 评分 API 路由                     | scorer               |
| `backend/models/video.py`                 | 视频数据模型                      | -                    |
| `backend/services/ffmpeg.py`              | FFmpeg 服务（自动下载、处理）     | models/video         |
| `backend/services/task_queue.py`          | 切割任务队列                      | ffmpeg               |
| `backend/api/video.py`                    | 视频 API 路由                     | ffmpeg, task_queue   |
| `frontend/src/components/CutProgress.jsx` | 切割进度展示组件                  | -                    |
| `frontend/src/components/CutProgress.css` | 进度组件样式                      | -                    |
| `output/`                                 | 视频导出目录，存放切割后的片段    | -                    |
| `.gitignore`                              | Git 忽略规则                      | -                    |
| `README.md`                               | 项目说明，快速上手指南            | -                    |

### 模块依赖关系

```
┌─────────────────────────────────────────────────────────────┐
│                         main.py                              │
│                    (FastAPI 应用入口)                         │
├──────────────────────────┬──────────────────────────────────┤
│                          │                                   │
│    config.py             │         api/ 路由层               │
│   (配置管理)              │                                   │
│                          ├── subtitle.py (/api/subtitle/*)   │
│                          │        │                          │
│                          │        ▼                          │
│                          │   services/ 服务层                │
│                          │   ├── subtitle.py (解析逻辑)      │
│                          │   │        │                      │
│                          │   │        ▼                      │
│                          │   └── models/ 数据层              │
│                          │       └── subtitle.py (数据结构)  │
└──────────────────────────┴──────────────────────────────────┘
```

**三层架构**：

- **API 层** (`api/`): 处理 HTTP 请求，参数验证，响应格式化
- **服务层** (`services/`): 核心业务逻辑，与框架解耦
- **模型层** (`models/`): 数据结构定义，用于验证和序列化

### 关键技术决策

#### 1. 为什么选择 Web 而非桌面应用？

- **开发效率**：Web 技术栈更快速迭代
- **跨平台**：一次开发，Windows/Mac/Linux 都能用
- **维护成本**：前后端分离，便于独立更新
- **未来扩展**：可轻松升级为在线服务

#### 2. 为什么本地优先？

- **隐私保护**：视频内容不离开用户电脑
- **响应速度**：无网络延迟
- **离线可用**：不依赖网络稳定性
- **成本控制**：减少云端 API 调用费用

#### 3. 视频处理策略

- 使用 FFmpeg 进行无损切割（-c copy）
- 需要转码时才重新编码
- 大文件采用流式处理，避免内存溢出

---

## 📊 性能目标

| 指标            | 目标                       |
| --------------- | -------------------------- |
| 字幕解析        | < 1 秒                     |
| AI 分析（本地） | < 30 秒（30 分钟视频字幕） |
| 视频切割        | < 5 秒/片段（无损切割）    |
| 首屏加载        | < 2 秒                     |

---

## 🚀 开发阶段

| 阶段        | 内容                          | 预计时间 |
| ----------- | ----------------------------- | -------- |
| **Phase 1** | 基础框架 + 字幕解析 + 简单 UI | 2-3 天   |
| **Phase 2** | AI 分析集成 + 高光提取        | 2-3 天   |
| **Phase 3** | 视频切割 + 导出功能           | 2 天     |
| **Phase 4** | UI 美化 + 用户体验优化        | 2 天     |

---

_文档版本: v1.0_  
_创建日期: 2025-12-15_
